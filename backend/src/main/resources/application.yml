spring:
  profiles:
    active:
      - prod
    group:
      local:
        - db-local
      prod:
        - db-aws
      docker-local:
        - db-docker-local
    include:
      - db
server:
  port: 8081